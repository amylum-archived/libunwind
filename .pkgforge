name 'libunwind'
org 'amylum'

deps(
  'linux-api-headers': '4.10-1'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

patch 'musl-execinfo-fix.patch'
patch 'gcc_s.patch'

build do
  run 'autoreconf -i'
  configure
  make
  install
end

test do
  # TODO: add tests
end
