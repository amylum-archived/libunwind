name 'libunwind'
org 'amylum'

deps(
  'linux-api-headers': '4.9-rc4-1'
)

configure_flags(
  prefix: '/usr'
)

cflags
harden

patch 'musl-execinfo-fix.patch'

build do
  run 'autoreconf -i'
  configure
  make
  install
end

test do
  # TODO: add tests
end
