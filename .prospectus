Prospectus.extra_dep('file', 'prospectus_pkgforge')

item do
  expected do
    url_xpath
    url 'http://www.nongnu.org/libunwind/download.html'
    xpath '//*[@id="col2"]'
    regex /.*libunwind-([\d.]+)\.tar\.gz.*/m
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end

  extend ProspectusPkgforge::DepItems
end
